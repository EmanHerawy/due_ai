# =============================================================================
# Due AI Web3 MCP Server + OpenClaw Configuration
# Copy to .env and fill in your values, then run ./setup.sh
# =============================================================================

# --- LLM Provider (set at least one) ---

# Default model to use (examples below)
# Anthropic:  anthropic/claude-sonnet-4-5-20250929, anthropic/claude-haiku-4-5-20251001, anthropic/claude-opus-4-5-20251101
# OpenAI:     openai/gpt-4o, openai/gpt-4o-mini, openai/o1
# Google:     google/gemini-2.0-flash, google/gemini-2.5-pro
# Ollama:     ollama/qwen2.5:14b, ollama/llama3.1:8b, ollama/mistral-small (local, free)
OPENCLAW_MODEL=anthropic/claude-sonnet-4-5-20250929

# API keys — set the one(s) matching your model provider (Ollama needs no key)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
# Ollama — local models via OpenAI-compatible API (no API key needed)
# Set to "true" to enable, then set OPENCLAW_MODEL=ollama/<model-name>
# Requires: ollama serve running on your machine
OLLAMA_ENABLED=
# Override if Ollama runs on a different host/port
OLLAMA_HOST=http://host.docker.internal:11434

# --- Sui ---

# Sui network: mainnet, testnet, devnet
SUI_NETWORK=testnet

# Optional LI.FI API key for higher rate limits
LIFI_API_KEY=

# --- OpenClaw Gateway ---

# Gateway auth token (auto-generated by setup.sh if empty)
OPENCLAW_GATEWAY_TOKEN=

# Gateway port
OPENCLAW_GATEWAY_PORT=18789

# --- Channels (configure the ones you need, leave others empty) ---

# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=

# Discord bot token from Discord Developer Portal
DISCORD_BOT_TOKEN=

# WhatsApp — uses QR pairing via `docker compose run --rm openclaw-cli channels login`
# Set allowed phone numbers (comma-separated, E.164 format e.g. +15555550123)
WHATSAPP_ALLOW_FROM=

# --- Channel Policies ---
# Options: pairing, allowlist, open, disabled
TELEGRAM_DM_POLICY=open
DISCORD_DM_POLICY=open
WHATSAPP_DM_POLICY=open
